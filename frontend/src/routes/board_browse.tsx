import { Board, Post } from '../main.tsx';
import { FC } from 'react';

type Props = {
    'board': Board
    'threads': Array<Post>
}


const BoardBrowse: FC<Props> = (props: Props) => (
    <></>
)

export default BoardBrowse
